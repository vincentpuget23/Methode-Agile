{% block body %}
	<div class"form">
		<form method="post" action="{{ path('hebergeur_delete', {'id': hebergeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
			<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hebergeur.id) }}">
			<div class="center">
				<button class="btn btn-danger">Supprimer</button>
			</div>
		</form>
	</div>
	<style>
		.center {
			text-align: center;
		}
	</style>
{% endblock %}
